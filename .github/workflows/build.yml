name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    
    - name: configure
      run: cmake -H. -Bbuild
    
    - name: build
      run: cmake --build build

    - name: run
      run: ./build/TypeScriptPP -s lua
